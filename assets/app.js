const menu=document.getElementById('menu'),nav=document.getElementById('nav');menu?.addEventListener('click',()=>{const o=nav?.classList.toggle('open')||false;menu.setAttribute('aria-expanded',String(o))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const modes={Research:'Research mode will combine web/file retrieval, citations, comparison and synthesis behind explicit evidence labels.',Engineering:'Engineering mode will plan, edit, test and review code with repository-scoped permissions and verification gates.',Knowledge:'Knowledge OS will connect documents, projects, structured memory and retrieval while keeping ownership and deletion controls explicit.',Media:'Creative Studio will coordinate image, video, audio, document and presentation workflows with clear generation provenance.',Agents:'Agent Workspace will run multi-step plans with approval checkpoints before consequential external writes.',Automation:'Automation Hub will support scheduled and conditional workflows with transparent cadence, scope and stop controls.'};document.querySelectorAll('[data-mode]').forEach(b=>b.addEventListener('click',()=>{document.getElementById('demoPrompt').textContent=b.dataset.mode+' workspace';document.getElementById('demoAnswer').textContent=modes[b.dataset.mode];document.querySelector('.command')?.scrollIntoView({behavior:'smooth',block:'center'})}));document.getElementById('launchDemo')?.addEventListener('click',()=>document.querySelector('.command')?.scrollIntoView({behavior:'smooth',block:'center'}));if('serviceWorker'in navigator)addEventListener('load',()=>navigator.serviceWorker.register('sw.js'));